services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data/downloads:/app/downloads
      - ./data/logs:/app/logs
      - ./downloads:/media
