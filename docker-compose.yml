services:
  bot:
    build: .
    container_name: mirror-bot
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - jellyfin
    ports:
      # Torrent selector
      - "8000:8000"
      # Google Drive search pages
      - "8001:8001"
      # Temporary local file explorer
      - "8003:8003"
      # Temporary Google Drive folder share pages
      - "8004:8004"
    volumes:
      - ./data/downloads:/app/downloads
      - ./data/logs:/app/logs
      - ./credentials.json:/app/data/google/credentials.json:ro
      - ./token.pickle:/app/data/google/token.pickle
      - ./downloads:/media
      - /var/run/docker.sock:/var/run/docker.sock

  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    restart: unless-stopped
    ports:
      - "8002:8096"
    volumes:
      - ./data/jellyfin/config:/config
      - ./data/jellyfin/cache:/cache
      - ./downloads:/media:ro
    environment:
      - TZ=${TZ:-Asia/Kolkata}
